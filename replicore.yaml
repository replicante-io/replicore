events:
  backend: sqlite
  path: replicore.sqlite

store:
  backend: sqlite
  path: replicore.sqlite

tasks:
  service:
    backend: sqlite
    path: replicore.sqlite

telemetry:
  logs:
    level: debug
    mode: terminal
  #otel:
  #  enabled: true
