events:
  backend: sqlite
  path: replicore.sqlite

store:
  backend: sqlite
  path: replicore.sqlite

tasks:
  service:
    backend: sqlite
    path: replicore.sqlite
    poll_delay_s: 5

telemetry:
  logs:
    level: debug
    levels:
      <unknown>: info
    mode: terminal
  #otel:
  #  enabled: true
