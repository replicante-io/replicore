coordinator:
  backend: sqlite
  path: replicore.sqlite

exclusives:
  candidate: true

events:
  backend: sqlite
  path: replicore.sqlite
  retention:
    age: 1

store:
  backend: sqlite
  path: replicore.sqlite

tasks:
  service:
    backend: sqlite
    path: replicore.sqlite
    poll_delay_s: 5

telemetry:
  logs:
    level: debug
    levels:
      <unknown>: info
    mode: terminal
  #otel:
  #  enabled: true
