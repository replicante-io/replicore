{
  "title": "OAction Apply Object",
  "description": "RepliCore's OAction Apply API payload",
  "type": "object",
  "properties": {
    "approved": {
      "description": "Indicate if an action is pre-approved or not. If unset a server specific default is used",
      "type": "boolean"
    },
    "spec": {
      "type": "object",
      "properties": {
        "ns_id": { "type": "string" },
        "cluster_id": { "type": "string" },
        "action_id": { "type": "string" },
        "args": { "type": "object" },
        "kind": { "type": "string" },
        "metadata": {
          "type": "object",
          "patternProperties": {
            ".*": { "type": "string" }
          }
        },
        "timeout": { "type": "string" }
      },
      "required": ["ns_id", "cluster_id", "kind"]
    }
  },
  "required": [
    "spec"
  ]
}
