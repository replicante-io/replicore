{
  "title": "ClusterSpec Apply Object",
  "description": "RepliCore's ClusterSpec Apply API payload",
  "type": "object",
  "properties": {
    "spec": {
      "type": "object",
      "properties": {
        "ns_id": { "type": "string" },
        "cluster_id": { "type": "string" },
        "enabled": { "type": "boolean" },
        "interval": { "type": "integer" }
      },
      "required": ["ns_id", "cluster_id"]
    }
  },
  "required": [
    "spec"
  ]
}
