[package]
name = "replicore-context"
version = "0.1.0"

edition = "2021"
rust-version = "1.78"

description = "RepliCore operation specific context"
homepage = "https://www.replicante.io/"
license = "MIT"

[features]
# Enable utilities for unit testing.
test-fixture = []

[dependencies]
actix-web = "^4.0"
opentelemetry = "^0.27"
slog = "^2.7"

replisdk = { version = "^0.1", features = ["replicore-models"] }
