[package]
name = "replicore-oaction-test"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"

description = "RepliCore orchestration actions to aid testing and system exploration"
homepage = "https://www.replicante.io/"
license = "MIT"

[dependencies]
anyhow = "^1.0"
async-trait = "^0.1"
serde_json = "^1.0"

replisdk = { version = "^0.1", features = [
  "replicore-models",
  "utils-error_json",
] }

replicore-context = { path = "../../../core/context"}
replicore-oaction = { path = "../../../core/oaction" }
