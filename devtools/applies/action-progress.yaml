apiVersion: replicante.io/v0
kind: AgentAction

metadata:
  namespace: default
  cluster: dev-agent-zookeeper
  node: https://localhost:3181
  approval: granted
  #approval: required

spec:
  action: replicante.debug.progress
  # TODO: convert kind format into the below
  #action: replicante.io/debug.progress
  args:
    test: true
    parameter: 12
